# ========= Linux-Specific Fish Configuration =========
# Add Linux-specific tools, paths, and abbreviations here.

# Example: Linuxbrew (uncomment if using Homebrew on Linux)
# set --global --export HOMEBREW_PREFIX "/home/linuxbrew/.linuxbrew"
# fish_add_path --global --move --path "$HOMEBREW_PREFIX/bin" "$HOMEBREW_PREFIX/sbin"
